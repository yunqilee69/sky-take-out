<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id, name, price, status, description, image, create_user, create_time, update_user, update_time) VALUE
        (#{categoryId}, #{name}, #{price}, #{status}, #{description}, #{image}, #{createUser}, #{createTime}, #{updateUser}, #{updateTime})
    </insert>

    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select s.*, c.name categoryName from setmeal s left join category c on c.id = s.category_id
        <where>
            <if test="name != null" >name like concat('%', #{name}, '%')</if>
            <if test="status != null">status = #{status}</if>
            <if test="categoryId != null">category_id = #{categoryId}</if>
        </where>
    </select>
</mapper>
